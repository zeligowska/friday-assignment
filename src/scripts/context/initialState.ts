import { CarSelectionState } from "../model/CarSelectionState";

export const initialState: CarSelectionState = {
    makesList: null,
    selectedMake: null,
    modelsList: null,
    selectedModel: null,
    carsList: null
}